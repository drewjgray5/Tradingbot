[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "--maxfail=1"

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = ["artifacts", "validation_artifacts", "mirofish_sims", "__pycache__"]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
# Imports after sys.path bootstrap in these entrypoints are intentional.
extend-per-file-ignores = { "full_report.py" = ["E402"], "run_auth.py" = ["E402"], "run_dual_auth.py" = ["E402"], "scripts/**" = ["E402"] }

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
warn_unused_ignores = true
exclude = "(artifacts|validation_artifacts|mirofish_sims|__pycache__)"
explicit_package_bases = true
namespace_packages = true
# Staged expansion beyond tests; ratchet this list over time.
files = ["tests", "webapp/security.py", "webapp/cors_config.py", "webapp/saas_redis.py"]
follow_imports = "skip"
disable_error_code = ["import-untyped"]
